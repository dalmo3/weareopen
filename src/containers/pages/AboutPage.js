import React from 'react';
import {Typography} from '@material-ui/core';

const AboutPage = (props) => (
  <div id="help">
    <Typography>More info soon...</Typography>
  </div>
);

export default AboutPage;
